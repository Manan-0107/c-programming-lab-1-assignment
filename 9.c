#include <stdio.h>

int main() {
    float $=48,a;
    float b;
    printf("enter the rupees which you want to convert into dollar");
    scanf("%f",&a);
    b=a/$;
    printf("the amount in dollar %f",b);
    return 0;
}