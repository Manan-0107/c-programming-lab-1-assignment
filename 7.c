#include <stdio.h>

int main() {

    float a,b;
    printf("enter minutes which you required to convert into hour");
    scanf("%f",&a);
    b=a/60;
    printf("the value of minutes convert into hour is %f",b);
    return 0;
}