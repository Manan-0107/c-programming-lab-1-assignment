#include <stdio.h>

int main() {

    int a,b;
    printf("enter hour which you required to convert into minutes");
    scanf("%d",&a);
    b=a*60;
    printf("the value of hour convert into minutes is %d",b);
    return 0;
}