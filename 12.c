#include <stdio.h>

int main() {
    int a,b;
    printf("enter the kilogram which you want to convert into gram");
    scanf("%d",&a);
    b=(a*1000);
    printf("the amount in kilogram %d",b);
    return 0;
}