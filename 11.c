#include <stdio.h>

int main() {
    float a,b;
    printf("enter the gram which you want to convert into kilogram");
    scanf("%f",&a);
    b=(a/1000);
    printf("the amount in kilogram %f",b);
    return 0;
}