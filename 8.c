#include <stdio.h>

int main() {
    int $=48,a,b;
    printf("enter the dollar which you want to convert into rupees");
    scanf("%d",&a);
    b=a*$;
    printf("the amount in rupees %d",b);
    return 0;
}